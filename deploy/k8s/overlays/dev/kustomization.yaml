apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# CI bumps `newTag` here on every push to main; Argo CD then syncs the cluster.
resources:
  - ../../base

images:
  - name: ghcr.io/han-sang-min/deploy-pilot
    newTag: latest

replicas:
  - name: deploy-pilot
    count: 2
