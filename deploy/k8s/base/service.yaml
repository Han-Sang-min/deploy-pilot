apiVersion: v1
kind: Service
metadata:
  name: deploy-pilot
  labels:
    app.kubernetes.io/name: deploy-pilot
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: deploy-pilot
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
